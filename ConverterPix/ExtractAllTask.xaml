<base:BaseWindow 
	x:Class="SCS_Mod_Helper.ConverterPix.ExtractAllTask"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:local="clr-namespace:SCS_Mod_Helper.ConverterPix"
	xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
	xmlns:base="clr-namespace:SCS_Mod_Helper.Base"
	ExtendsContentIntoTitleBar="True"
	d:DataContext="{d:DesignInstance Type=local:ExtractAllBinding}"
	mc:Ignorable="d"
	WindowStartupLocation="CenterOwner"
	Width="300"
	SizeToContent="Height"
	MinWidth="300"
	MinHeight="10">
	<Grid
		x:Name="GridMain">
		<Grid.RowDefinitions>
			<RowDefinition
				Height="Auto" />
			<RowDefinition
				Height="Auto" />
		</Grid.RowDefinitions>
		<ui:TitleBar
			Title="{DynamicResource TitleEDF}" />
		<StackPanel
			Grid.Row="1"
			Margin="15,0,15,10">
			<TextBlock
				x:Name="TextMessage"
				Text="{DynamicResource MessageGatherFiles}"/>
			<ProgressBar
				x:Name="ProgressStep"
				Margin="0,15,0,0"
				IsIndeterminate="True"
				Value="0"
				Maximum="7"/>
			<StackPanel
				x:Name="PanelFile">
				<StackPanel.Style>
					<Style
						TargetType="StackPanel">
						<Style.Triggers>
							<DataTrigger
								Binding="{Binding PrepareMode, Mode=OneWay}"
								Value="true">
								<Setter
									Property="Visibility"
									Value="Collapsed" />
							</DataTrigger>
						</Style.Triggers>
					</Style>
				</StackPanel.Style>
				<TextBlock
					HorizontalAlignment="Right"><Run
						Text="{Binding Value, ElementName=ProgressStep}" /><Run 
						Text="/"/><Run 
						Text="{Binding Maximum, ElementName=ProgressStep}" />
				</TextBlock>
				<StackPanel>
					<TextBlock
						x:Name="TextCurrentFileType"
						Text="PMD File:" />
					<TextBlock
						x:Name="TextCurrentFile"
						Text="CurrentFile" 
						TextWrapping="Wrap"/>
				</StackPanel>
				<ProgressBar
					x:Name="ProgressFile"
					Margin="0,5,0,0"
					Value="0"
					Maximum="100" />
				<TextBlock
					HorizontalAlignment="Right"><Run
						Text="{Binding Value, ElementName=ProgressFile}" /><Run
						Text="/" /><Run
						Text="{Binding Maximum, ElementName=ProgressFile}" />
				</TextBlock>
			</StackPanel>
			<Grid
				Margin="0,15,0,0">
				<Grid.ColumnDefinitions>
					<ColumnDefinition
						Width="*" />
					<ColumnDefinition
						Width="Auto" />
				</Grid.ColumnDefinitions>
				<Button
					Grid.Column="1"
					Padding="20,5"
					Content="{DynamicResource ButtonCancel}"
					Click="ButtonCancelClick" />
			</Grid>
		</StackPanel>
	</Grid>
</base:BaseWindow>
