<base:BaseWindow
	x:Class="SCS_Mod_Helper.Accessory.Physics.PhysicsWindow"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:local="clr-namespace:SCS_Mod_Helper.Accessory.Physics"
	xmlns:base="clr-namespace:SCS_Mod_Helper.Base"
	xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
	d:DataContext="{d:DesignInstance Type=local:PhysicsBinding}"
	mc:Ignorable="d"
	Width="900"
	Height="Auto"
	WindowStartupLocation="CenterOwner"
	SizeToContent="Height"
	Loaded="OnLoaded">
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition
				Height="Auto" />
			<RowDefinition
				Height="*" />
		</Grid.RowDefinitions>
		<ui:TitleBar
			Title="{DynamicResource TitlePhysicsToyData}" />
		<Grid
			x:Name="GridMain"
			Grid.Row="1"
			Margin="10,0,10,10">
			<Grid.RowDefinitions>
				<RowDefinition
					Height="Auto" />
				<RowDefinition
					Height="*" />
				<RowDefinition
					Height="Auto" />
			</Grid.RowDefinitions>
			<StackPanel
				Margin="0,0,0,5"
				Grid.ColumnSpan="3">
				<TextBlock
					Text="{DynamicResource MsgPhysics1}" />
				<TextBlock
					x:Name="TextMsgPhysics2"
					Margin="0,5,0,0"
					Text="{DynamicResource MsgPhysics2}">
				</TextBlock>
			</StackPanel>

			<ScrollViewer
				Grid.Row="1">
				<local:PhysicsUC
					x:Name="UCPhysics"
					LocalPhysics="True"
					d:Height="433" />
			</ScrollViewer>

			<StackPanel
				x:Name="PanelSelection"
				Grid.Row="2"
				Margin="0,10,0,0"
				Orientation="Horizontal"
				FlowDirection="RightToLeft">
				<Button
					x:Name="ButtonSelect"
					Content="{DynamicResource ButtonSelect}"
					Padding="50,5,50,5"
					Margin="0,0,5,0"
					Click="ButtonResultClick">
					<Button.Style>
						<Style
							TargetType="Button"
							BasedOn="{StaticResource {x:Type Button}}">
							<Style.Triggers>
								<DataTrigger
									Binding="{Binding CurrentPhysicsItem, ElementName=UCPhysics}"
									Value="{x:Null}">
									<Setter
										Property="IsEnabled"
										Value="False" />
								</DataTrigger>
							</Style.Triggers>
						</Style>
					</Button.Style>
				</Button>
				<Button
					x:Name="ButtonCancel"
					Content="{DynamicResource ButtonCancel}"
					Padding="50,5,50,5"
					Click="ButtonResultClick" />
			</StackPanel>
		</Grid>
	</Grid>
</base:BaseWindow>
